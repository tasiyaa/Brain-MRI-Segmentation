1. Several model coverage: Several model coverage: Three architectures spanning the evolution of U-Net-based segmentation: the original encoder-decoder, nested dense skip connections (U-Net++), and hybrid CNN + self-attention (Swin-UNet Lite).
2. Principled evaluation: Clinically relevant evaluation using five metrics including boundary-aware HD95.
